import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  logo: {
    position: "absolute",
    top: 20,
    right: 50,
    width: 100,
    height: 100,
    resizeMode: "contain",
  },
});

export default styles;
