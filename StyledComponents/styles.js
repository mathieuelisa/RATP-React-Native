import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerButton: {
    width: "70%",
  },
  myButton: {
    backgroundColor: "red",
    borderRadius: 20,
    alignItems: "center",
  },
  text: {
    color: "purple",
  },
});

export default styles;
